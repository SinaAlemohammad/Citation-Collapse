Optional. Normal runs do NOT read this - they use the canonical seed.jsonl at the
repo root. This raw source (title, abstract, citation_count per line) is only
needed if you want to regenerate a fresh standardized seed from scratch via
utils/data_prep/crawler.py.
